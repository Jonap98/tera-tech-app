import 'dart:typed_data';

import 'package:flutter/material.dart';

class SolicitudesProvider extends ChangeNotifier {
  Future<bool> uploadImage(String path, Uint8List bytes) async {
    // try {
    // final resp = await C
    // } catch (e) {}

    return true;
  }
}
